Seismology data stored in inventory table

Fields/Table for original JSON data

Quadrangle data from DGGS populated into GMC

Place data from USGS populated into GMC

Barcodes are numeric? Are all barcodes stored numerically?
